% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lake.R
\name{lake_tbl}
\alias{lake_tbl}
\title{A lazy table reference into the CourseKata data lake.}
\usage{
lake_tbl(table, schema = "ck_datalake_prd_marts_app")
}
\arguments{
\item{table}{Name of the table in the data lake.}

\item{schema}{Database the table lives in; defaults to the marts database.}
}
\value{
A lazy dbplyr \code{tbl}.
}
\description{
Returns a lazy dbplyr \code{tbl} bound to a package-managed Athena connection, so you never open or
close a connection yourself. The result is an ordinary dbplyr \code{tbl}: pipe it through dplyr
verbs (\code{filter()}, \code{count()}, ...), \code{collect()} or \code{\link[=collect_cached]{collect_cached()}} it, \code{show_query()} it,
or join two \code{lake_tbl()} results together — they share the one managed connection.
}
